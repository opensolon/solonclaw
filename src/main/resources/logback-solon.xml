<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE configuration>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <conversionRule conversionWord="tags" converterClass="ch.qos.logback.solon.SolonTagsConverter" />

    <solonProperty name="APP_NAME" source="solon.app.name" defaultValue="solon-claw" />
    <solonProperty name="RUNTIME_HOME" source="solonclaw.runtime.home" defaultValue="runtime" />
    <property name="LOG_DIR" value="${RUNTIME_HOME}/logs" />

    <solonProperty name="CONSOLE_LOG_PATTERN" source="solon.logging.appender.console.pattern"
                   defaultValue="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{48} - %msg%n%ex" />
    <solonProperty name="CONSOLE_LOG_LEVEL" source="solon.logging.appender.console.level"
                   defaultValue="INFO" />

    <solonProperty name="FILE_LOG_PATTERN" source="solon.logging.appender.file.pattern"
                   defaultValue="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{48} - %msg%n%ex" />
    <solonProperty name="FILE_LOG_LEVEL" source="solon.logging.appender.file.level"
                   defaultValue="INFO" />
    <solonProperty name="FILE_LOG_MAX_HISTORY" source="solon.logging.appender.file.maxHistory"
                   defaultValue="7" />
    <solonProperty name="FILE_LOG_MAX_FILE_SIZE" source="solon.logging.appender.file.maxFileSize"
                   defaultValue="10 MB" />
    <solonProperty name="LOGGER_ROOT_LEVEL" source="solon.logging.logger.root.level"
                   defaultValue="INFO" />

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <immediateFlush>true</immediateFlush>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${CONSOLE_LOG_LEVEL}</level>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="AgentFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${FILE_LOG_LEVEL}</level>
        </filter>
        <file>${LOG_DIR}/agent.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/agent.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${FILE_LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${FILE_LOG_MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="GatewayFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>${FILE_LOG_LEVEL}</level>
        </filter>
        <file>${LOG_DIR}/gateway.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/gateway.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${FILE_LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${FILE_LOG_MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="ErrorFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <file>${LOG_DIR}/errors.log</file>
        <append>true</append>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/errors.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${FILE_LOG_MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${FILE_LOG_MAX_HISTORY}</maxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="Solon" class="ch.qos.logback.solon.SolonAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>TRACE</level>
        </filter>
    </appender>

    <logger name="com.jimuqu.solon.claw.gateway" level="INFO" additivity="false">
        <appender-ref ref="Console" />
        <appender-ref ref="GatewayFile" />
        <appender-ref ref="ErrorFile" />
        <appender-ref ref="Solon" />
    </logger>

    <logger name="com.jimuqu.solon.claw.scheduler" level="INFO" />
    <logger name="com.jimuqu.solon.claw.tool" level="INFO" />

    <root level="${LOGGER_ROOT_LEVEL}">
        <appender-ref ref="Console" />
        <appender-ref ref="AgentFile" />
        <appender-ref ref="ErrorFile" />
        <appender-ref ref="Solon" />
    </root>
</configuration>
